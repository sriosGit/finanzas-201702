<%= form_with(model: entry_type, local: true) do |form| %>
  <% if entry_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry_type.errors.count, "error") %> prohibited this entry_type from being saved:</h2>

      <ul>
      <% entry_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :entry_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
